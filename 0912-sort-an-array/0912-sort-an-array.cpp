class Solution {
public:
    vector<int> sortArray(vector<int>& nums) {
        sort(nums.begin(),nums.end());
        return nums;
        // int low=0;
        // int high=nums.size();
        // while(low<high){
        //     int mid=(low+high)/2;

        // }

        // )
    }
};